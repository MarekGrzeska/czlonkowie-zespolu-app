interface TeamMember {
    id: number,
    name: string,
    email: string,
    phoneNumber: string,
    photoUrl: string,
    isActive: boolean,
    createdDate: string,
}

export default TeamMember;